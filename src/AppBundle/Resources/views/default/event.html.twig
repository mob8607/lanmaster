{% extends '@App/base.html.twig' %}

{% block body %}
    <div>
        <table class="striped">
            <thead>
            <tr>
                <th>Gesamtrang</th>
                <th>Nickname</th>
            {% for key in results|keys %}
                <th>{{ key }}</th>
            {% endfor %}
            </tr>
            </thead>

            <tbody>
                <tr>
                    {% for key in results %}
                    {#<td>{{ result.playerId }}</td>#}
                    {#<td>{{ result.matchId}}</td>#}
                    {% endfor %}
                </tr>

            </tbody>
        </table>
    </div>
{% endblock %}
