{% extends '@App/base.html.twig' %}

{% block body %}
    <div id="wrapper" class="container">
        <div class="row">
            <div class="col s12">
                <h2>
                    {{ settings.title }}
                </h2>
            </div>
        </div>

        <div class="row">
            <div class="col s12">
                Wähle ein Event:

                <div class="collection">
                    {% for event in events %}
                        <a href="{{ path('event', {'eventId': event.id}) }}" class="collection-item">
                            {{ event.name }}
                        </a>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
